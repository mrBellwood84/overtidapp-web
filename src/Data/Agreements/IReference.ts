/** model for reference in agreement rule */
export interface IReference {
    /** entity id */
    id: string;
    /** reference text */
    text: string;
    /** text languge for localization */
    lang: string;
}